#include <bits/stdc++.h>
using namespace std;

int main()
{
	string s;
	getline(cin, s, '$');

	/*

	getline function also has a third parameter
	which is optional , it can be used as a terminator

	*/

	cout << s;
	return 0;
}